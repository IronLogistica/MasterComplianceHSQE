[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python brand_library.py && python import_library.py && python seed.py && gunicorn 'app:create_app()'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
